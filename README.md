# age-gitlab-tool-tui

Install dependencies:

```bash
go get github.com/rivo/tview
```

Setup environment variables:

```bash
GITLAB_URL=...
GITLAB_TOKEN=...

```

Run the application:

```bash
go run main.go
```
